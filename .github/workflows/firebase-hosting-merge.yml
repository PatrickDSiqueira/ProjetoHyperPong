# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - test
      - master
jobs:
  build_and_deploy_test:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/test'
    steps:
      - uses: actions/checkout@v3
      - name: create .env file
        run: |
          echo "REACT_APP_API_KEY=AIzaSyD4320HYZ9pCIw5Ngadiq12gdWvac5eDJE" >> .env
          echo "REACT_APP_AUTH_DOMAIN=testhyperpongeventos.firebaseapp.com" >> .env
          echo "REACT_APP_PROJECT_ID=testhyperpongeventos" >> .env
          echo "REACT_APP_STORAGE_BUCKET=testhyperpongeventos.appspot.com" >> .env
          echo "REACT_APP_MESSAGING_SENDER_ID=586944534722" >> .env
          echo "REACT_APP_APP_ID=1:586944534722:web:b12b58d8b5f7471702c35b" >> .env
      - name: npm install and build
        run: npm install && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_TESTHYPERPONGEVENTOS }}'
          channelId: live
          projectId: testhyperpongeventos

  build_and_deploy_master:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master'
    steps:
      - uses: actions/checkout@v3
      - run: npm install && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_HYPERPONGEVENTOS }}'
          channelId: live
          projectId: hyperpongeventos
